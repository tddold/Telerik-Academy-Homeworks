namespace MobileDevice
{
    using System;
    using System.Collections.Generic;

    public class GSMCallHistoryTest
    {
        //public static void Main()
        //{
        //    // Comment the Main() in GSMTest.cs and uncomment this to test
        //    GSM mobilePhone = new GSM("dsds", "dsds");

        //    Call currentCall = new Call(DateTime.Now, "0899587433", 284);
        //    mobilePhone.AddCalls(currentCall);

        //    currentCall = new Call(DateTime.Now.AddMinutes(788), "0899410033", 100);
        //    mobilePhone.AddCalls(currentCall);

        //    currentCall = new Call(DateTime.Now.AddDays(14), "0877410033", 1440);
        //    mobilePhone.AddCalls(currentCall);

        //    foreach (var item in mobilePhone.CallHistory)
        //    {
        //        Console.WriteLine("Date: {0}\nNumber: {1}\nDuration: {2}\n", item.TimeOfCall, item.PhoneNumber, item.Duration);
        //    }

        //    Console.WriteLine("Price of calls: {0:C2}", mobilePhone.CalculateCallsPrice());

        //    long maxCallDuration = long.MinValue;
        //    Call longestCall = new Call(DateTime.Now, "0000000000", 0);

        //    foreach (var item in mobilePhone.CallHistory)
        //    {
        //        if (item.Duration > maxCallDuration)
        //        {
        //            maxCallDuration = item.Duration;
        //            longestCall = item;
        //        }
        //    }

        //    mobilePhone.DeleteCalls(longestCall);
        //    Console.WriteLine("The price without the longest call: {0:C2}", mobilePhone.CalculateCallsPrice());

        //    mobilePhone.ClearCalls();

        //    Console.WriteLine("\nCheck for calls:");

        //    if (mobilePhone.CallHistory.Count == 0)
        //    {
        //        Console.WriteLine("No calls found");
        //    }
        //}
    }
}
